[概要]
汎用ビューのひとつ。
template_nameを設定するだけ（一行で）でビューを完成することができるが、
それは関数ビューでも同じような気がする。労力はさしてそこまでかわらない。
変数を渡すこともでき、その場合、get_context_dataメソッドをオーバーライドする。
urls.pyもクラスビューなのでas_view()を使わないといけないなど、
使ってみた感触ではどちらかに統一したほうが混乱がなさそう。

[詳細な使い方]
なし